Create virtual environment (with venv as the virtual env name):
python -m venv venv

Activate virtual environment:
venv\Scripts\activate

Deactivate virtual environment:
deactivate/exit

Run FastAPI app:
uvicorn app:app --host 127.0.0.1 --port 5000